# # Environment Variables
# DATABASE_URL=postgres://user:password@localhost:5432/skillbridge
# JWT_SECRET=your-secret-key
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret
# FRONTEND_URL=http://localhost:5173
#
# Local dev: callback uses frontend URL so GitHub redirects to 5173 and Vite proxy forwards to backend.
# Set GITHUB_CALLBACK_URL only if you want to override (e.g. direct backend URL).
# In GitHub OAuth App set Authorization callback URL to: http://localhost:5173/api/github/auth/github/callback